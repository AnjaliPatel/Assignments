//This class tests the complete all installment function which is invoked through a custom button.
@isTest
private class CompleteAllInstallmentsClassTest {
	//This method tests the completion of all the open installments for a donation. 
    static testMethod void testAllOpenInstallments() {
        
    }
}